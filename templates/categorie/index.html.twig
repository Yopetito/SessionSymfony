{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
<h1>CATEGORIES</h1>
<h2>Choisir une categorie pour acceder aux differents modules</h2>

{% if categories is empty %}
    "Il n'y a pas de catégories disponibles"
{% else %}
    

    

    {% for categorie in categories %}
        <a href="{{ path('app_modules', {'id':categorie.id}) }}">{{ categorie }}
            <a href=" {{path('delete_categorie', {'id':categorie.id})}}"><i class="fa-solid fa-trash"></i></a>
        </a>
        <br>
    {% endfor %}
    <a href="{{ path('add_module') }}">ajouter un module</a>
{% endif %}


{% endblock %}
